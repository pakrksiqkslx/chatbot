# 벡터화 및 벡터 DB를 위한 패키지 (numpy 2.x 호환 버전)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
faiss-cpu
pinecone>=5.0.0
# sentence-transformers는 이미 설치됨
# huggingface-hub는 이미 설치됨
# tqdm는 이미 설치됨
# numpy 2.3.2는 이미 설치됨

# 한국어 임베딩 모델
# jhgan/ko-sroberta-multitask 자동 다운로드됨

